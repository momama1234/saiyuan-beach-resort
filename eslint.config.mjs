/**
 * ESLint configuration for apps/web-andalay
 * Uses shared Next.js config from @repo/eslint-config
 */
import nextjsConfig from '@repo/eslint-config/nextjs'

const config = [...nextjsConfig]

export default config
